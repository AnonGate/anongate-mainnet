{
  "name": "@absolute-privacy/relayer",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Local Sepolia withdraw relayer — broadcasts client-built calldata only (no note secrets).",
  "scripts": {
    "start": "node ./start-both.mjs",
    "start:both": "node ./start-both.mjs",
    "start:sepolia": "node ./src/server.mjs --env-file=.env.sepolia",
    "start:mainnet": "node ./src/server.mjs --env-file=.env.mainnet",
    "dev": "node ./start-both.mjs",
    "test": "node --test ./src/allowlist.test.mjs"
  },
  "dependencies": {
    "viem": "^2.31.0"
  }
}
